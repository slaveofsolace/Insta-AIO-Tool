# 3.1.8 acceptance

Instagram can return the displayed number of relationship rows while repeating identities around pagination boundaries. A native Following traversal returned 130 rows but only 127 unique account IDs. A supported 50-row traversal exposed a different 128 unique IDs. Their union contained exactly 130 valid, nonconflicting identities.

Version 3.1.8 keeps the guided Followers, close, Following, close sequence. If either native capture ends short, the same initiated check performs one authenticated pagination pass and merges only exact observed usernames. It accepts a list only when the merged unique count equals the unchanged exact profile total. Nonterminal pagination, changed totals, conflicts, challenges, rate limits, and incomplete unions remain partial.

Fresh verification passes assembly, generated-userscript parity, repository hygiene, all 481 tests, extension and userscript Chromium acceptance, all 45 reviewed Windows overlay states, and all 11 reviewed Windows PWA states. Screenshot tolerances are unchanged.

The exact-count authenticated run of the 3.1.8 userscript is still pending. No Follow, Unfollow, or Unsend mutation is part of this acceptance.
