# 3.1.8 acceptance

Instagram's native Followers request was observed redirecting to the homepage and returning HTML instead of relationship JSON. The cause of that redirect is unknown; it is not a proven rate-limit response. The native list also recycles rendered rows, so one scroll sweep can miss accounts even after reaching the bottom.

Version 3.1.8 keeps the guided Followers, close, Following, close sequence and uses Instagram's native list requests throughout. If a sweep ends short, the checker revisits that dialog with tighter overlapping scroll steps while retaining unique observed usernames. It accepts a list only when the unique count equals the unchanged exact profile total. Changed totals, conflicts, challenges, rate limits, and incomplete lists remain partial.

The earlier background-reader checkpoint passed assembly, generated-userscript parity, repository hygiene, all 505 tests, extension and userscript Chromium acceptance, Chrome pairing, all 45 reviewed Windows overlay states, and all 11 reviewed Windows PWA states. Native-button fixtures wait for delayed initial rows, handle a hidden indicator before a visible loader, collect 45 recycled rows per list while excluding 30 recommendations, and preserve the saved comparison when a native list is unavailable or its loader stalls. Background-button fixtures cover both cursor chains, partial-save preservation, restriction stops, and zero native-list clicks. Screenshot tolerances are unchanged.

Focused regressions cover a dialog mounted before its rows or scroller, a loading indicator that clears on the final wait, bounded initial-load and pagination-loader timeouts, Stop during either wait, rate-limit detection while waiting, and persistent resweep progress. These tests establish reader behavior, not recovery of Instagram's missing native data.

The installed candidate was verified against its source SHA-256 before authenticated testing. The full comparison failed: Instagram's native Followers request returned an HTTP 302 redirect to the homepage, followed by HTML instead of relationship JSON. The dialog displayed an empty-list notice and suggested profiles. Following also remained short of its displayed total.

That run exposed a capture defect: recommendations were counted as followers. The corrected reader excludes links after the recommendation heading and rejects an empty native list when the profile total is positive.

## Installed background reader

The commit-pinned userscript from `e67c369b8c3e13ee4d0d3db1406da0aa62499c5b` was reinstalled and tested through **Capture lists and export → Check in background** on September 8, 2026. Its SHA-256 is `3ae82b7e50e90010720dc7bb2c7abd1fe727d4c0821a18f0627346325a498346`. No native list was opened.

The first run returned 2,026 of 2,070 followers and all 126 following. One instrumented repeat produced the same result:

| List | Pages | Returned unique accounts | Declared total | Final continuation |
| --- | ---: | ---: | ---: | --- |
| Followers | 57 | 2,026 | 2,070 | None |
| Following | 3 | 126 | 126 | None |

All 60 list responses were successful JSON responses. The first follower page's 44 accounts were retained. There were no duplicate identities or invalid usernames, and every returned account was reflected in the tool's aggregate result. Both terminal responses explicitly reported no next page; declared totals stayed unchanged.

The follower shortfall therefore exists in the returned cursor chain, not in DOM virtualization or dropped rows in this reader. The reason Instagram declares 44 more followers than it returns is not established. These results do not prove those accounts are deactivated, restricted, or otherwise unavailable, and they do not establish complete follower membership.

The checker withheld the comparison and preserved its saved data. Full authenticated comparison acceptance and release promotion remain pending. No Follow, Unfollow, or Unsend mutation was performed. Network evidence is aggregate-only; account identities and response bodies are not included.

## Explicit follow-back review

A separate bounded diagnostic returned 127 unique Following accounts against a declared total of 126. Every returned account had strict boolean relationship fields: 109 followed back and 18 did not. This is direct per-account evidence, not proof that the complete Following membership was returned. It also shows why profile-counter equality alone cannot validate individual relationships.

The candidate now keeps verified follow-back reviews separate from saved full-list comparisons. Both surfaces show incomplete coverage explicitly, provide review downloads, and exclude the transient review from action sources. The primary button reads in the background; the native scan remains under Advanced. The settled loading bar is hidden.

Assembly, generated userscript rebuilding, and all 515 tests pass. Focused tests cover absent and malformed flags, conflicting duplicates, wrong or changed viewers, counters above and below returned counts, misleading matching counts, follower-side proof, and preservation of saved comparisons. Chromium fixtures cover the review UI in both surfaces, including a returned count above the declared count and restoration of saved data after a restriction.

The full extension/userscript fixture suite, Chrome pairing, 11 PWA states, and 45 overlay states pass. Four intentional layout/copy deltas were visually reviewed before regenerating the baselines; thresholds are unchanged. The review fixture uses pointer input on visible controls. Production dependency audit reports no advisories; one existing moderate development-only xmldom advisory remains in the desktop build chain. Browser and web archive contents are verified.

## Installed verified review

The commit-pinned userscript from `590b944612893920ac91233d4b03d96338f14146` completed one authenticated background check on September 8, 2026. Its SHA-256 is `40ad2a42105882cebdc0699a7147e4e0ea9416eb24ebe49cd6803c6d22cf4133`. No native list dialog was opened.

The review displayed 109 verified mutuals and 18 verified non-followers. Instagram returned 2,027 Followers against 2,070 declared and 127 Following against 126 declared. Both groups were labeled partial. The primary button returned to idle, the loading indicator cleared, and the saved comparison remained unchanged.

All 18 non-followers appeared in the list. Selecting Mutuals showed 25 of 109 accounts; **Show 25 more** displayed 50 without rescanning. The opposite-direction group correctly showed no verified accounts and an incomplete-list explanation, rather than subtracting incomplete Following data.

The downloaded UTF-8 report contained 127 numbered entries in separate sections, the recorded time, both coverage totals, and explicit partial labels. No account identities or private report contents are included here.

[CI run 34269427460](https://github.com/slaveofsolace/Insta-Toolbox/actions/runs/34269427460) passed all five jobs, including Windows install/launch/uninstall checks, macOS universal packaging, overlay QA, tests, and release checksums. CodeQL and dependency review also passed. The tested PR merge tree matched the source tree. Hosted userscript, extension, and web checksums matched the local artifacts; the downloaded checksum archive passed its envelope hash and ZIP integrity checks.

This establishes the installed verified-review flow, not complete follower membership. The missing follower identities have not been recovered. Full-list acceptance and release promotion remain pending. No account mutation was performed.
