# 3.1.8 acceptance

Instagram's native list requests carry live web-session headers. A request without those headers can return the full Instagram HTML page with HTTP 200 instead of relationship JSON. The native list also recycles rendered rows, so one scroll sweep can miss accounts even after reaching the bottom.

Version 3.1.8 keeps the guided Followers, close, Following, close sequence and uses Instagram's native list requests throughout. If a sweep ends short, the checker revisits that dialog with tighter overlapping scroll steps while retaining unique observed usernames. It accepts a list only when the unique count equals the unchanged exact profile total. Changed totals, conflicts, challenges, rate limits, and incomplete lists remain partial.

Fresh verification passes assembly, generated-userscript parity, repository hygiene, all 486 tests, extension and userscript Chromium acceptance, Chrome pairing, all 45 reviewed Windows overlay states, and all 11 reviewed Windows PWA states. Both primary-button fixtures collect 45 recycled rows per list while excluding 30 recommendations, and preserve the saved comparison when a native list is unavailable. Screenshot tolerances are unchanged.

The installed candidate was verified against its source SHA-256 before authenticated testing. The full comparison failed: Instagram's native Followers request returned an HTTP 302 redirect to the homepage, followed by HTML instead of relationship JSON. The dialog displayed an empty-list notice and suggested profiles. Following also remained short of its displayed total.

That run exposed a capture defect: recommendations were counted as followers. The corrected reader excludes links after the recommendation heading and rejects an empty native list when the profile total is positive. The installed corrected reader and a complete authenticated comparison still require acceptance. No Follow, Unfollow, or Unsend mutation was performed, and release promotion remains pending.
