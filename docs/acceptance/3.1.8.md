# 3.1.8 acceptance

Instagram's native Followers request was observed redirecting to the homepage and returning HTML instead of relationship JSON. The cause of that redirect is unknown; it is not a proven rate-limit response. The native list also recycles rendered rows, so one scroll sweep can miss accounts even after reaching the bottom.

Version 3.1.8 keeps the guided Followers, close, Following, close sequence and uses Instagram's native list requests throughout. If a sweep ends short, the checker revisits that dialog with tighter overlapping scroll steps while retaining unique observed usernames. It accepts a list only when the unique count equals the unchanged exact profile total. Changed totals, conflicts, challenges, rate limits, and incomplete lists remain partial.

Fresh verification passes assembly, generated-userscript parity, repository hygiene, all 505 tests, extension and userscript Chromium acceptance, Chrome pairing, all 45 reviewed Windows overlay states, and all 11 reviewed Windows PWA states. Both primary-button fixtures wait for delayed initial rows, handle a hidden indicator before a visible loader, collect 45 recycled rows per list while excluding 30 recommendations, and preserve the saved comparison when a native list is unavailable or its loader stalls. Background-button fixtures cover both cursor chains, partial-save preservation, restriction stops, and zero native-list clicks. Screenshot tolerances are unchanged.

Focused regressions cover a dialog mounted before its rows or scroller, a loading indicator that clears on the final wait, bounded initial-load and pagination-loader timeouts, Stop during either wait, rate-limit detection while waiting, and persistent resweep progress. These tests establish reader behavior, not recovery of Instagram's missing native data.

The installed candidate was verified against its source SHA-256 before authenticated testing. The full comparison failed: Instagram's native Followers request returned an HTTP 302 redirect to the homepage, followed by HTML instead of relationship JSON. The dialog displayed an empty-list notice and suggested profiles. Following also remained short of its displayed total.

That run exposed a capture defect: recommendations were counted as followers. The corrected reader excludes links after the recommendation heading and rejects an empty native list when the profile total is positive.

## Installed background reader

The commit-pinned userscript from `e67c369b8c3e13ee4d0d3db1406da0aa62499c5b` was reinstalled and tested through **Capture lists and export → Check in background** on September 8, 2026. Its SHA-256 is `3ae82b7e50e90010720dc7bb2c7abd1fe727d4c0821a18f0627346325a498346`. No native list was opened.

The first run returned 2,026 of 2,070 followers and all 126 following. One instrumented repeat produced the same result:

| List | Pages | Returned unique accounts | Declared total | Final continuation |
| --- | ---: | ---: | ---: | --- |
| Followers | 57 | 2,026 | 2,070 | None |
| Following | 3 | 126 | 126 | None |

All 60 list responses were successful JSON responses. The first follower page's 44 accounts were retained. There were no duplicate identities or invalid usernames, and every returned account was reflected in the tool's aggregate result. Both terminal responses explicitly reported no next page; declared totals stayed unchanged.

The follower shortfall therefore exists in the returned cursor chain, not in DOM virtualization or dropped rows in this reader. The reason Instagram declares 44 more followers than it returns is not established. These results do not prove those accounts are deactivated, restricted, or otherwise unavailable, and they do not establish complete follower membership.

The checker withheld the comparison and preserved its saved data. Full authenticated comparison acceptance and release promotion remain pending. No Follow, Unfollow, or Unsend mutation was performed. Network evidence is aggregate-only; account identities and response bodies are not included.
