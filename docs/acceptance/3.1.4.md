# 3.1.4 acceptance

DM Unsend and read-only conversation checks cancel when the open thread changes. The shared runner observes DOM and navigation changes and checks the thread during active waits. Returning to the original thread cannot resume the canceled run.

- 370 tests passed for the source fix, including cancellation without navigation events, zero-removal interruption, and a fresh run without reloading.
- Chromium extension and generated-userscript acceptance passed, including virtualized DM traversal and Stop after a verified removal.
- The release requires green CI on its exact main commit, including 45 overlay states, 11 PWA states, Chrome pairing, Windows/macOS packaging, and archive checksums.
- Existing reviewed screenshot baselines retain their original capture versions; this patch changes no layout.

Firefox authenticated behavior has not been reproduced locally. No live Instagram mutation was performed for this patch. Windows remains unsigned; macOS packages are ad-hoc signed and not notarized.
